# Authentication API

## Using existing Plan 1.3
Completed tasks:
- Installed `jsonwebtoken` and `cookie`.
- Created `src/lib/jwt.ts` with `signToken` and `verifyToken`.
- Created `/api/auth/login` endpoint with Zod validation.
- Created `/api/auth/me` endpoint for session verification.
