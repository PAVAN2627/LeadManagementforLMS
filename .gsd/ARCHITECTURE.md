# Architecture

> Auto-generated by /map on 2026-02-18

## Overview

High-fidelity React application serving as a Lead Management System (LMS). It features role-based access control for Admins, Managers, and Agents, each with their own dashboards and specific workflows.

```
┌─────────────────────────────────────────┐
│               App.tsx                   │
│          (React Router v6)              │
│       /admin | /manager | /agent        │
├─────────────────────────────────────────┤
│           Role-Based Pages              │
│   (AdminDashboard, ManagerDashboard...) │
├─────────────────────────────────────────┤
│           Shared Components             │
│        (Layouts, Tables, UI)            │
├─────────────────────────────────────────┤
│             Data Layer                  │
│       (Imports from mockData.ts)        │
└─────────────────────────────────────────┘
```

## Components

### Core Pages
- **Admin**: `src/pages/admin/` (Dashboard, Leads, Users, Reports, Settings)
- **Manager**: `src/pages/manager/` (Dashboard, Leads, Team, Reports)
- **Agent**: `src/pages/agent/` (Dashboard, MyLeads, AddLead)
- **Auth**: `src/pages/Login.tsx`

### Layouts
- **DashboardLayout**: `src/components/layout/DashboardLayout.tsx`
- **ManagerLayout**: `src/components/layout/ManagerLayout.tsx`

### UI System
- **Location**: `src/components/ui/`
- **Library**: Shadcn/UI (Radix Primitives + Tailwind)
- **Icons**: Lucide React

## Data Flow

Current implementation is a **Frontend Prototype** relying on static data.

1. **Source**: `src/data/mockData.ts` exports static arrays (`mockLeads`, `mockUsers`, etc.).
2. **Consumption**: Pages import these arrays directly.
3. **State**: Local React state (`useState`) is used for UI interaction (modals, filters). 
4. **Fetching**: `QueryClientProvider` is configured in `App.tsx` but effectively unused for data fetching as no API integration exists.

## Integration Points

| Service | Type | Purpose | Status |
|---------|------|---------|--------|
| None | N/A | No backend integration | Pending |

## Technical Debt

- [ ] **Hardcoded Data**: Application relies entirety on `mockData.ts`.
- [ ] **No Authentication**: `Login.tsx` exists but likely lacks real auth logic/session management.
- [ ] **Duplicate Logic**: Similar dashboard logic split across three role directories (Admin/Manager/Agent) could be refactored.
- [ ] **Unused Query Client**: React Query is setup but not utilized for remote data.

## Conventions

- **Naming**: PascalCase for components, camelCase for variables.
- **Structure**: Feature-based grouping in `pages/` (admin/manager/agent).
- **Styling**: Tailwind utility classes + CSS variables in `index.css`.
